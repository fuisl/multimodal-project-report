% !TeX root = ../paper.tex
\subsection{Evaluation Framework}
\label{sec:evaluation}

\paragraph{Evaluation Overview}

With the improving generalization capabilities of \ac{LLM}s, a growing trend in evaluation is the use of the \emph{LLM-as-judge} paradigm~\cite{llmasjudge}. Instead of relying on domain-specific human experts for manual evaluation, an LLM judge automatically grades model outputs based on predefined prompts and evaluation metrics. While this approach may be less reliable in terms of absolute correctness and trustworthiness compared to expert human evaluation, well-designed metrics and instructions can provide high flexibility, fast evaluation speed, and rapid insights into system performance. In this work, we adopt the RAGAS framework to conduct LLM-as-judge evaluations using both standardized built-in metrics and custom-defined metrics.

\paragraph{Evaluation Metrics}

The input JSON used for evaluation contains a subset of the following fields, depending on the metrics being applied:
\begin{itemize}
  \item \texttt{question}: the test case
  \item \texttt{ground\_truth}: the ground-truth final diagnosis
  \item \texttt{ground\_truth\_reasoning}: the ground-truth reasoning
  \item \texttt{contexts}: contexts retrieved by the \ac{RAG} system
  \item \texttt{answer}: the model’s final diagnosis
  \item \texttt{answer\_reasoning}: the model’s reasoning
\end{itemize}

However, the RAGAS framework internally supports only four standardized fields:
\begin{itemize}
  \item \texttt{user\_input}
  \item \texttt{response}
  \item \texttt{retrieved\_context}
  \item \texttt{reference}
\end{itemize}

As a result, we decompose the evaluation pipeline into three separate stages to assess different aspects of system performance: RAG, answer, and reasoning.

The summary of all the used metrics is illustrated in Table~\ref{tab:metric_summary}.

\newcommand{\var}[1]{\texttt{\textit{#1}}}

\begin{table*}[t]
\centering
\caption{Summary of Evaluation Metrics}
\label{tab:metric_summary}
\renewcommand{\arraystretch}{1.2}
\setlength{\tabcolsep}{4pt}
\begin{tabular}{p{3cm}p{3cm}p{8cm}p{2cm}}
\hline
\textbf{Metric Name} &
\textbf{Purpose} &
\textbf{Meaning} &
\textbf{Provider} \\
\hline

Context Recall &
RAG Evaluation &
Measures whether all claims in \var{ground\_truth} and \var{ground\_truth\_reasoning} are supported in the retrieved \var{contexts} &
RAGAS \\

Context Precision &
RAG Evaluation &
Measures whether the retriever ranks relevant chunks higher than irrelevant ones in the retrieved \var{contexts} &
RAGAS \\

Faithfulness &
Answer Evaluation &
Measures whether the \var{answer} is factually grounded in the provided \var{contexts} &
RAGAS \\

Answer Relevance &
Answer Evaluation &
Measures whether the \var{answer} is relevant to the given \var{question} &
Custom \\

Answer Accuracy &
Answer Evaluation &
Measures whether the \var{answer} matches the \var{ground\_truth} &
Custom \\

Reasoning Recall &
Reasoning Evaluation &
Measures whether the \var{answer\_reasoning} covers all key elements of the \var{ground\_truth\_reasoning} &
Custom \\

\hline
\end{tabular}
\end{table*}

\paragraph{Evaluation Settings}

We conduct evaluations on retrieval, final answers, and reasoning using a benchmark dataset comprising 78 cases. The LLM-as-judge selected for this evaluation is \texttt{MedGemma-4b-Instruct}. Although common practice recommends using a judge model that is different from the evaluated models and ideally from a different model family to mitigate bias, the requirement for strong medical domain knowledge motivates our choice of a model pre-trained on medical datasets to ensure reliable evaluation.

For each test case, contextual information is retrieved once and then shared across all model settings during inference to ensure a fair comparison while reducing computational overhead. RAG system employs the \texttt{bge-m3} embedding model and \texttt{MedGemma-4b} as the retrieval model. Given a text-only query, it retrieves the top 20 most relevant data chunks. Each evaluated model is expected to produce a single final diagnosis along with the corresponding reasoning. In total, we evaluate six diagnostic settings using three models, including \texttt{MedGemma-4b}, \texttt{Gemma-3-4b-it}, and \texttt{Gemma-3-4b-it-finetuned}, each evaluated in two modes: with \ac{RAG} and without \ac{RAG}. The RAG-supported configurations are denoted by the suffix “+ RAG” in the model name.

\paragraph{Evaluation Results}

We first evaluate the performance of the multimodal RAG system. As shown in Table~\ref{tab:lightrag_results}, LightRAG+RagAnything achieves a high Context Recall score of approximately 0.9, indicating effective retrieval of relevant information. However, the Context Precision score remains moderate, at below 0.6, suggesting that retrieved contexts are not optimally ranked by relevance.

\begin{table}[htbp]
\centering
\caption{Multimodal RAG Retrieval Performance. For all metrics, higher scores indicate better performance with minimum score is 0 and maximum score is 1.}
\label{tab:lightrag_results}
\setlength{\tabcolsep}{2pt} % default is 6pt
\begin{tabular}{lccc}
\hline
\textbf{Method} &
\textbf{Output} &
\textbf{Context Recall} &
\textbf{Context Precision} \\
\hline
LightRAG+RagAnything &
Top-20 Chunks &
0.8932 &
0.5651 \\
\hline
\end{tabular}
\end{table}

Next, we evaluate the answer and reasoning performance of each model setting. The detailed scores, reported in Table~\ref{tab:eval_answer_reasoning}, show that all settings achieve relatively similar results on the Faithfulness and Answer Relevance metrics, with the maximum difference between the best- and worst-performing models being approximately 0.1 for each metric. In contrast, Answer Accuracy and Reasoning Recall exhibit a wider range of performance across different settings. These results are further visualized using bar charts for more exploratory analysis in Fig.~\ref{fig:eval_graph_4} and Fig.~\ref{fig:eval_graph_avg}.

\begin{figure}[htbp]
  \centering
  \includegraphics[height=0.6\linewidth]{figures/eval_graph_4_metrics.png}
  \caption{Comparison of each configuration across the four evaluation metrics}
  \label{fig:eval_graph_4}
\end{figure}

\begin{figure}[htbp]
  \centering
  \includegraphics[height=0.4\linewidth]{figures/Eval_graph_avg.jpg}
  \caption{Average performance across the four metrics by configuration}
  \label{fig:eval_graph_avg}
\end{figure}

\begin{table*}[t]
\centering
\caption{Performance comparison of models with and without \ac{RAG}. The best and second-best results for each metric are highlighted in \textbf{BOLD} and \underline{underlined}, respectively. For all metrics, higher scores indicate better performance with minimum score is 0 and maximum score is 1.}
\label{tab:eval_answer_reasoning}
\renewcommand{\arraystretch}{1.2}
\begin{tabular}{lc|cccc}
\hline
\textbf{Models} &
\textbf{Output} &
\textbf{Faithfulness} &
\textbf{Answer Accuracy} &
\textbf{Answer Relevance} &
\textbf{Reasoning Recall} \\
\hline

\texttt{medGemma-4b}
& Answer + Reasoning
& 0.6837 & 0.1562 & \textbf{0.7051} & 0.4333 \\

\texttt{medGemma-4b} + RAG
& Answer + Reasoning
& \underline{0.7225} & 0.2466 & 0.6167 & 0.3979 \\

\texttt{gemma3-4b-it}
& Answer + Reasoning
& 0.6944 & 0.1053 & 0.6167 & 0.4887 \\

\texttt{gemma3-4b-it} + RAG
& Answer + Reasoning
& \textbf{0.7512} & \textbf{0.3421} & 0.5859 & \underline{0.6434} \\

\texttt{gemma3-4b-it-finetuned}
& Answer + Reasoning
& 0.6313 & 0.0938 & \underline{0.6359} & \textbf{0.6564} \\

\texttt{gemma3-4b-it-finetuned} + RAG
& Answer + Reasoning
& 0.7130 & \underline{0.3333} & 0.6051 & 0.6165 \\

\hline
\end{tabular}
\end{table*}






